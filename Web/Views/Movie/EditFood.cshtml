@model MoviesService.Dto.GenresDto
@{
    ViewBag.Title = "EditFood";
}

<h2>EditFood asfasdfasdfadsfasdfasfasdfasfasfasfas</h2>

@using (Html.BeginForm("UpdateFood", "Movie", FormMethod.Post))
{
    <fieldset>
        @Html.HiddenFor(m => m.Id)
        <p>
            @Html.LabelFor(m => m.Name, "Название книги")
            <br />
            @Html.EditorFor(m => m.Name)
        </p>
        <p><input type="submit" value="Отправить" /></p>
    </fieldset>
}