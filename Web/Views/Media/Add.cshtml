@model MoviesService.Dto.MediaDto

@using (Html.BeginForm())
{
    <div class="authorization-position">
        <table>
            <tr>
                <th style="width:200px"> @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "authorization-label" })</th>
                <th> @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "authorization-login" } })</th>
            </tr>
            <tr>
                <th style="width:200px"> @Html.LabelFor(model => model.Year, htmlAttributes: new { @class = "authorization-label" })</th>
                <th> @Html.EditorFor(model => model.Year, new { htmlAttributes = new { @class = "authorization-login" } })</th>
            </tr>
            <tr>
                <th style="width:200px"> @Html.LabelFor(model => model.Plot, htmlAttributes: new { @class = "authorization-label" })</th>
                <th> @Html.EditorFor(model => model.Plot, new { htmlAttributes = new { @class = "authorization-login" } })</th>
            </tr>
            <tr>
                <th>Genres</th>
                <th>
                    @foreach (var c in ViewBag.Genres)
                    {
                        <input type="checkbox" name="selectedGenres" value="@c.Id" />@c.Name
                        <br />
                    }
                </th>
            </tr>
            <tr>
                <th>Countries</th>
                <th>
                    @foreach (var c in ViewBag.Country)
                    {
                        <input type="checkbox" name="selectedCountries" value="@c.Id" />@c.Name
                        <br />
                    }
                </th>
            </tr>
            <tr>
                <th>Types</th>
                <th>
                    @foreach (var c in ViewBag.Types)
                    {
                        <input type="radio" name="selectedType" value="@c.Id" />@c.Name
                        <br />
                    }
                </th>
            </tr>
        </table>

        <input type="submit" value="Add" class="authorization-button"/>
    </div>
}